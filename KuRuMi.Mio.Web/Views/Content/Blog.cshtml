
@{
    ViewBag.Title = "博客";
    Layout = "~/Views/Shared/BlogPage.cshtml";
}
<div class="container">
    <div class="row">
        <div class="col-md-2">
            <div class="row">
                <div class="thumbnail">
                    <img src="~/Images/街景1.jpg" />
                    <span class="pull-right">——<small class="text-info ">测试</small></span>
                </div>
                <div>
                    123456
                </div>

            </div>
        </div>
        <div class="col-md-10">
        </div>
    </div>
</div>
@section script{
    <script>
        var ctrl = new Init();
        var ctrls = new Init();
        var data = { "": localStorage.userinfo };
        var queryData = { id: localStorage.userinfo };
        var result = ctrl.ajaxApi(data, "Api/Blogs/GetBlogsInfo");
        var userInfo = ctrls.ctrlAjax(queryData, "/Content/GetUserInfoById");
        if (userInfo == "请先登录!") {
            window.location.href = "/Home/Index";
        }
        else {
            $.each(result, function (n, item) {
                console.log(item.title);
            });
        }

    </script>
}
